using CCGKit;
using UnityEngine;

namespace GameArchitecture
{
    /// <summary>
    /// The card library manager. Responsible for handling card libraries during runtime.
    /// </summary>
    
    [CreateAssetMenu(menuName = "Runtime Systems/Card Library Manager",fileName = "CardLibraryManager")]
    public class CardLibraryManager : ScriptableObject
    {
        /*
        [SerializeField, Tooltip("The card library assigned as the starter deck for the run.")]
        private CardLibrary starterDeck;

        [SerializeField, Tooltip("The runtime variable used to save the current state of the deck for the run.")]
        private CardLibrary runDeck;

        [SerializeField, Tooltip("The runtime variable used to save the current state of the card library during an encounter.")]
        private CardLibrary encounterDeck;
        
        [SerializeField,Tooltip("The runtime variable used to save the current")]
*/
    }
}
